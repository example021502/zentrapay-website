# react-placeholder@4.1.0's peerDependencies only list React 16/17, but it
# works fine with React 19 (it's plain divs/spans, no legacy API usage) — this
# avoids `npm install`/`npm ci` failing on that peer-dependency conflict.
legacy-peer-deps=true
